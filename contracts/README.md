# SoulBlocks Smart Contract

ERC-721 contract with append-only fragment storage (code-as-storage). Deployed on Base.

Built with [Foundry](https://book.getfoundry.sh/).

## Build & Test

```bash
forge build
forge test
```

See `src/SoulBlocks.sol` for the main contract.
