# SoulBlocks Website

The frontend for [soulblocks.ai](https://soulblocks.ai). Built with Next.js, wagmi, and viem.

## Local Development

```bash
npm install
npm run dev
```

Requires a `.env.local` file — see `.env.example` for required variables.
